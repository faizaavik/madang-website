---
// Hero Section Component
---

<!-- Hero Section / Beranda -->
<header
  id="beranda"
  class="hero-gradient h-[600px] flex items-center justify-center text-center text-white px-4"
>
  <style>
    .hero-gradient {
      background:
        linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)),
        url("/gambar-1.jpg");
      background-size: cover;
      background-position: center;
    }
  </style>
  <div>
    <h1 class="text-4xl md:text-6xl font-bold mb-4">
      Cari Rasa Terbaik di Yogyakarta
    </h1>
    <p class="text-lg md:text-xl mb-8 max-w-2xl mx-auto">
      Temukan hidden gems kuliner mulai dari Gudeg legendaris hingga Sate
      Klatak yang nikmat.
    </p>

    <!-- Pencarian & Filter -->
    <div
      class="bg-white p-4 rounded-xl shadow-2xl flex flex-col md:flex-row space-y-3 md:space-y-0 md:space-x-3 max-w-4xl mx-auto"
    >
      <div class="flex-1 relative">
        <i
          class="fas fa-utensils absolute left-3 top-3.5 text-gray-400"
        ></i>
        <input
          type="text"
          placeholder="Cari makanan (misal: Gudeg)..."
          class="w-full pl-10 pr-4 py-3 rounded-lg border focus:outline-none focus:ring-2 focus:ring-orange-500 text-gray-800"
        />
      </div>
      <div class="flex-1 relative">
        <i
          class="fas fa-map-marker-alt absolute left-3 top-3.5 text-gray-400"
        ></i>
        <select
          class="w-full pl-10 pr-4 py-3 rounded-lg border focus:outline-none focus:ring-2 focus:ring-orange-500 text-gray-800 appearance-none"
        >
          <option value="">Pilih Lokasi</option>
          <option>Kota Yogyakarta</option>
          <option>Sleman</option>
          <option>Bantul</option>
          <option>Gunung Kidul</option>
        </select>
      </div>
      <button
        class="bg-orange-600 text-white px-8 py-3 rounded-lg font-bold hover:bg-orange-700 transition"
      >
        CARI
      </button>
    </div>
  </div>
</header>
